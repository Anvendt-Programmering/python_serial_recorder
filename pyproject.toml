[project]
name = "python-serial-recorder"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "darkdetect",
    "matplotlib",
    "openpyxl",
    "pandas",
    "pyserial",
    "pyside6",
]

[tool.uv]
link-mode = "copy"